"""Configuration package: paths, strategy parameters, and the trading universe.

All dates, tickers, FRED ids, currency settings, and the proxy map live here so
that the rest of the package (CLAUDE.md §5) never hard-codes a magic number.
"""
